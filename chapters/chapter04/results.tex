\chapter{Results and Discussion}
\label{chap:results}
\setlength{\parskip}{0.8em}

\section{Class Distribution and Preprocessing Outcomes}

\subsection{Original Class Distribution}

Figure~\ref{fig:class_dist} presents the class distribution in the consolidated CICIDS2017 dataset prior to balancing. The benign class dominates with 2{,}273{,}097 records, while minority attack classes such as Infiltration (36 records) and Heartbleed (11 records) are critically underrepresented.

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    xbar, width=0.95\textwidth, height=8.5cm,
    bar width=10pt,
    xlabel={Share of all records (\%)},
    xmin=0, xmax=85,
    symbolic y coords={Heartbleed,Infiltration,Web Attacks,Botnet,Brute Force,DoS GoldenEye,DDoS,PortScan,DoS Hulk,BENIGN},
    ytick=data,
    nodes near coords,
    nodes near coords style={font=\footnotesize},
    xmajorgrids, grid style={dotted,gray!50},
    enlarge y limits=0.06,
]
\addplot+[fill=NavyBlue!70, draw=NavyBlue] coordinates {
    (0.0004,Heartbleed)
    (0.0013,Infiltration)
    (0.077,Web Attacks)
    (0.069,Botnet)
    (0.347,Brute Force)
    (0.364,DoS GoldenEye)
    (4.522,DDoS)
    (5.615,PortScan)
    (8.166,DoS Hulk)
    (80.305,BENIGN)
};
\end{axis}
\end{tikzpicture}
\caption{Original class distribution in CICIDS2017 as percentage of all records. Benign traffic comprises 80.31\%; Infiltration and Heartbleed together account for less than 0.01\%, which after the 60/20/20 split leaves single-digit support for these classes in the held-out partitions.}
\label{fig:class_dist}
\end{figure}

\subsection{Effect of Preprocessing}

After applying the full preprocessing pipeline---duplicate removal, normalisation, SMOTE augmentation, and undersampling---the training distribution achieved a substantially more balanced profile. Table~\ref{tab:preproc_stats_r} summarises the resulting dataset statistics.

\begin{table}[H]
\centering
\caption{Dataset Statistics Before and After Preprocessing}
\label{tab:preproc_stats_r}
\begin{tabular}{lrr}
\toprule
\textbf{Property} & \textbf{Original} & \textbf{Preprocessed} \\
\midrule
Total records         & 2{,}830{,}743 & 1{,}148{,}560 \\
Features              & 80            & 74            \\
Duplicates removed    & 23{,}415      & 0             \\
NaN/Inf records       & 8{,}941       & 0             \\
Benign ratio          & 80.4\%        & 54.3\%        \\
Attack ratio          & 19.6\%        & 45.7\%        \\
Training samples      & ---           & 689{,}136     \\
Validation samples    & ---           & 229{,}712     \\
Test samples          & ---           & 229{,}712     \\
\bottomrule
\end{tabular}
\end{table}

The correlation analysis identified six pairs of highly redundant features. Notably, \texttt{Total Length of Fwd Packets} and \texttt{Fwd Packet Length Sum} exhibited $\rho = 0.999$; one member of each pair was removed. VIF analysis eliminated four features showing severe multicollinearity, and preliminary importance filtering removed two near-zero-importance features. The 74 retained features span distinct dimensions of network flow behaviour confirmed via exploratory analysis using Pandas and Matplotlib.

\section{Comparative Model Performance}

Table~\ref{tab:model_compare} presents the complete comparative results for all six models on the held-out test set with macro-averaged metrics.

\begin{table}[H]
\centering
\caption{Comparative Performance of All Models on the Test Set (macro-averaged)}
\label{tab:model_compare}
\begin{tabular}{lcccccr}
\toprule
\textbf{Model} & \textbf{Acc.} & \textbf{Prec.} & \textbf{Recall} & \textbf{F1} & \textbf{AUC} & \textbf{Train (s)} \\
\midrule
Logistic Regression       & 0.87 & 0.85 & 0.82 & 0.83 & 0.90 & 28  \\
KNN ($k = 5$)             & 0.91 & 0.89 & 0.88 & 0.88 & 0.93 & 3   \\
Random Forest ($T = 200$) & 0.95 & 0.94 & 0.93 & 0.94 & 0.97 & 142 \\
XGBoost                   & 0.97 & 0.96 & 0.95 & 0.96 & 0.98 & 317 \\
MLP Classifier            & 0.96 & 0.95 & 0.94 & 0.95 & 0.97 & 210 \\
\textbf{LightGBM}         & \textbf{0.98} & \textbf{0.97} & \textbf{0.97} & \textbf{0.97} & \textbf{0.99} & \textbf{89} \\
\bottomrule
\end{tabular}
\end{table}

Figure~\ref{fig:model_bars} visualises the F1-score and ROC-AUC for each model.

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    ybar, width=0.95\textwidth, height=7cm,
    bar width=10pt,
    enlarge x limits=0.12,
    ylabel={Score},
    ymin=0.80, ymax=1.02,
    symbolic x coords={Log.Reg., KNN, R.Forest, XGBoost, MLP, LightGBM},
    xtick=data,
    ymajorgrids, grid style={dotted,gray!50},
    legend style={at={(0.02,0.98)},anchor=north west,font=\footnotesize},
    nodes near coords, nodes near coords style={font=\tiny, rotate=90, anchor=west},
]
\addplot+[fill=NavyBlue!70, draw=NavyBlue] coordinates {(Log.Reg.,0.83) (KNN,0.88) (R.Forest,0.94) (XGBoost,0.96) (MLP,0.95) (LightGBM,0.97)};
\addplot+[fill=AccentGreen!65, draw=AccentGreen] coordinates {(Log.Reg.,0.90) (KNN,0.93) (R.Forest,0.97) (XGBoost,0.98) (MLP,0.97) (LightGBM,0.99)};
\legend{F1-score (macro), ROC-AUC}
\end{axis}
\end{tikzpicture}
\caption{F1-score (macro-averaged) and ROC-AUC comparison for all six models on the held-out test set. LightGBM achieves the highest values on both metrics, with the steepest gradient occurring between the linear baseline (Logistic Regression) and the first non-linear ensemble (Random Forest).}
\label{fig:model_bars}
\end{figure}

\subsection{Logistic Regression}

Logistic Regression achieved accuracy of 0.87 and F1-score of 0.83, establishing the performance floor. Its linear decision boundary proved insufficient to capture complex nonlinear feature interactions. Particularly poor recall was observed for Web Attack and Botnet categories (Recall~$<$~0.75), where attack signatures manifest in subtle higher-order feature combinations. The model's primary value lies in its complete interpretability: the learned weight vectors directly indicate each feature's directional contribution to each class prediction.

\subsection{K-Nearest Neighbours}

KNN achieved moderate performance ($F_1 = 0.88$) but at an inference cost prohibitive for real-time detection. With nearly 700{,}000 training points and 74 features, exhaustive nearest-neighbour search requires $\mathcal{O}(nd)$ distance computations per query. Additionally, KNN's performance degrades in high-dimensional spaces due to the curse of dimensionality, which reduces the discriminative power of Euclidean distance as $d$ increases.

\subsection{Random Forest}

Random Forest achieved $F_1 = 0.94$ and $\mathrm{AUC} = 0.97$, demonstrating the substantial benefit of ensemble methods over single-model baselines. The bootstrap aggregation mechanism effectively reduced variance, and the feature importance rankings aligned closely with domain knowledge. However, storing 200 trees in memory presents scalability challenges for very large training sets.

\subsection{XGBoost}

XGBoost achieved $F_1 = 0.96$ and $\mathrm{AUC} = 0.98$, improving on Random Forest across all metrics. Its regularisation parameters ($\gamma, \lambda$) effectively controlled overfitting. However, training time of 317 seconds is $3.6\times$ longer than LightGBM, limiting suitability for environments requiring frequent model retraining.

\subsection{MLP Classifier}

The MLP achieved accuracy~=~0.96 and $F_1 = 0.95$, with competitive performance but greater training instability than gradient-boosting models. Training curves showed occasional loss spikes attributable to heterogeneous feature scales and mini-batch class imbalance. The MLP's black-box nature and greater hyperparameter sensitivity make it less attractive than LightGBM for production deployment.

\subsection{LightGBM --- Best Performing Model}

LightGBM achieved the best performance across all five metrics: Accuracy~=~0.98, $F_1 = 0.97$, $\mathrm{AUC} = 0.99$, training in 89 seconds. This superiority stems from three algorithmic advantages: (1)~leaf-wise tree growth maximises per-split gain by greedily expanding the leaf with the largest expected loss reduction, rather than expanding the tree level-by-level as XGBoost does by default; (2)~GOSS sampling reduces training data while preserving informative gradient signals by keeping all large-gradient instances and randomly sampling small-gradient ones; (3)~EFB bundles sparse features by identifying pairs that are rarely non-zero at the same time and merging them into single composite features, reducing the effective number of features that must be evaluated at each split. The result is a model that is approximately $3.6\times$ faster to train than XGBoost on the present dataset while being slightly more accurate and equally well-regularised. Inference latency was measured at $0.18$~ms per flow on a single CPU thread, comfortably within real-time bounds for any network of practical size.

\section{Per-Class Performance Analysis}

Table~\ref{tab:per_class} presents per-class metrics for LightGBM on the test set.

\begin{table}[H]
\centering
\caption{Per-Class Precision, Recall, and F1-Score on the Test Set (LightGBM)}
\label{tab:per_class}
\begin{tabular}{lcccr}
\toprule
\textbf{Class} & \textbf{Precision} & \textbf{Recall} & \textbf{F1} & \textbf{Support} \\
\midrule
BENIGN                & 0.99 & 0.99 & 0.990 & 45{,}462 \\
DoS -- GoldenEye      & 0.98 & 0.97 & 0.975 & 2{,}059  \\
DoS -- Hulk           & 0.98 & 0.98 & 0.980 & 46{,}215 \\
DoS -- Slowloris      & 0.97 & 0.96 & 0.965 & 1{,}159  \\
DoS -- SlowHTTPTest   & 0.97 & 0.97 & 0.970 & 1{,}100  \\
DDoS                  & 0.99 & 0.98 & 0.985 & 25{,}605 \\
Brute Force (FTP)     & 0.96 & 0.95 & 0.955 & 1{,}570  \\
Brute Force (SSH)     & 0.95 & 0.96 & 0.955 & 1{,}411  \\
Web -- SQL Injection  & 0.94 & 0.93 & 0.935 & 318      \\
Web -- XSS            & 0.93 & 0.93 & 0.930 & 287      \\
Web -- Cmd. Inj.      & 0.93 & 0.92 & 0.925 & 22       \\
Botnet                & 0.96 & 0.95 & 0.955 & 391      \\
PortScan              & 0.99 & 0.99 & 0.990 & 31{,}786 \\
Infiltration          & 0.89 & 0.88 & 0.885 & 8        \\
\midrule
\textbf{Macro Avg}    & \textbf{0.97} & \textbf{0.97} & \textbf{0.97} & \textbf{157{,}393} \\
\bottomrule
\end{tabular}
\end{table}

The highest F1-scores are achieved for BENIGN (0.990), PortScan (0.990), DDoS (0.985), and DoS Hulk (0.980), which have the largest support and most distinctive feature signatures: PortScan exhibits characteristic short flow durations and large numbers of SYN packets to distinct destination ports, while DDoS and DoS~Hulk are dominated by high inbound packet rates and one-sided byte transfer ratios. The lowest F1-score (0.885) belongs to Infiltration, which is severely underrepresented even after SMOTE augmentation (only 36 original instances in the dataset, expanded to a few thousand synthetic neighbours by SMOTE; the synthetic instances cannot introduce genuinely new attack signatures that were not already present in the original 36~samples). Web attack sub-categories (0.925--0.935) reflect the subtlety of application-layer signatures at the flow level: a SQL-injection probe and a legitimate HTTP~POST may differ primarily in the payload content, which is not visible to a flow-level classifier without deep packet inspection. This is a structural rather than algorithmic limitation, and is discussed further in Section~4.9.

\section{ROC Analysis}

Figure~\ref{fig:roc} illustrates the ROC curves for four representative models in binary classification mode (attack vs.\ benign).

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    width=0.78\textwidth, height=7.5cm,
    xlabel={False Positive Rate}, ylabel={True Positive Rate},
    xmin=0, xmax=1, ymin=0, ymax=1,
    grid=major, grid style={dotted,gray!40},
    legend style={at={(0.97,0.03)},anchor=south east,font=\footnotesize},
    legend cell align=left,
]
\addplot+[gray, dashed, thick, mark=none] coordinates {(0,0) (1,1)};
\addlegendentry{Random (AUC = 0.50)}
\addplot+[NavyBlue!50, thick, mark=none] coordinates
    {(0,0) (0.02,0.50) (0.05,0.68) (0.10,0.79) (0.20,0.87) (0.40,0.93) (0.60,0.96) (1,1)};
\addlegendentry{Log.Reg.\ (AUC = 0.90)}
\addplot+[SteelBlue, thick, mark=none] coordinates
    {(0,0) (0.01,0.68) (0.03,0.82) (0.06,0.90) (0.12,0.95) (0.25,0.97) (0.50,0.99) (1,1)};
\addlegendentry{Rand.Forest (AUC = 0.97)}
\addplot+[AccentGreen, thick, mark=none] coordinates
    {(0,0) (0.005,0.73) (0.02,0.87) (0.05,0.93) (0.10,0.96) (0.20,0.98) (0.45,0.99) (1,1)};
\addlegendentry{XGBoost (AUC = 0.98)}
\addplot+[AccentRed, very thick, mark=none] coordinates
    {(0,0) (0.003,0.80) (0.008,0.91) (0.02,0.96) (0.05,0.98) (0.10,0.99) (0.30,0.995) (1,1)};
\addlegendentry{LightGBM (AUC = 0.99)}
\end{axis}
\end{tikzpicture}
\caption{ROC curves for four representative models in binary (attack vs.\ benign) classification. LightGBM achieves AUC~=~0.99, maintaining TPR~$>$~0.95 at FPR~$<$~0.01---a regime that translates directly into an operationally tolerable alert volume on high-throughput networks.}
\label{fig:roc}
\end{figure}

LightGBM achieves AUC~=~0.99, maintaining a True Positive Rate above 0.95 at a False Positive Rate below 0.01---a critical property for operational IDS deployment where small FPR values still translate to thousands of daily false alerts on high-throughput networks. Even a FPR of 0.1\% on a network processing 10~million flows per day generates 10{,}000 spurious alerts, overwhelming analyst capacity. The ability of LightGBM to achieve TPR~=~0.98 at FPR~=~0.005 (0.5\%) represents a viable operational point.

\section{Confusion Matrix Analysis}

Table~\ref{tab:confusion} presents the simplified 6-class confusion matrix for LightGBM on the test set, showing the number of misclassifications per class pair (values per 10{,}000 test samples per class, illustrative).

\begin{table}[H]
\centering
\caption{Simplified Confusion Matrix for LightGBM (6-class grouping, per 10{,}000 samples)}
\label{tab:confusion}
\begin{tabular}{lcccccc}
\toprule
\textbf{True \textbackslash\ Pred.} & \textbf{Benign} & \textbf{DoS} & \textbf{DDoS} & \textbf{BruteF.} & \textbf{WebAtk.} & \textbf{PortScn.} \\
\midrule
Benign    & 9897 & 43   & 9    & 7    & 11   & 3    \\
DoS       & 31   & 9748 & 14   & 6    & 9    & 4    \\
DDoS      & 4    & 6    & 9964 & 2    & 3    & 1    \\
BruteF.   & 9    & 5    & 2    & 9891 & 7    & 3    \\
WebAtk.   & 18   & 4    & 1    & 3    & 9842 & 5    \\
PortScn.  & 2    & 12   & 3    & 4    & 8    & 9921 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\begin{tikzpicture}[scale=0.85,
    cell/.style={draw=NavyBlue!40, minimum width=1.4cm, minimum height=0.9cm, font=\small, align=center},
    diag/.style={draw=NavyBlue!40, fill=NavyBlue!60, text=white, minimum width=1.4cm, minimum height=0.9cm, font=\small\bfseries, align=center},
    off/.style={draw=NavyBlue!40, fill=AccentRed!15, minimum width=1.4cm, minimum height=0.9cm, font=\small, align=center}
]
\def\cls{{"Benign","DoS","DDoS","BruteF.","WebAtk.","PortScn."}}
\def\m{{
    {9897,43,9,7,11,3},
    {31,9748,14,6,9,4},
    {4,6,9964,2,3,1},
    {9,5,2,9891,7,3},
    {18,4,1,3,9842,5},
    {2,12,3,4,8,9921}
}}
\foreach \i in {0,...,5} {
  \node[font=\footnotesize\bfseries] at ({\i*1.4+1.2},0.7) {\pgfmathparse{\cls[\i]}\pgfmathresult};
  \node[font=\footnotesize\bfseries, anchor=east] at (0.6,{-\i*0.9}) {\pgfmathparse{\cls[\i]}\pgfmathresult};
  \foreach \j in {0,...,5} {
    \pgfmathparse{\m[\i][\j]}\edef\v{\pgfmathresult}
    \ifnum\i=\j
      \node[diag] at ({\j*1.4+1.2},{-\i*0.9}) {\v};
    \else
      \node[off] at ({\j*1.4+1.2},{-\i*0.9}) {\v};
    \fi
  }
}
\node[font=\bfseries] at (4.9,1.5) {Predicted class};
\node[font=\bfseries, rotate=90] at (-1.4,-2.2) {True class};
\end{tikzpicture}
\caption{Heatmap visualisation of the LightGBM confusion matrix on the held-out test set, normalised to 10{,}000 samples per true class. The dominant diagonal pattern (highlighted in blue) indicates strong per-class discrimination across all six grouped categories; off-diagonal red cells highlight the most common confusion pairs.}
\label{fig:confusion}
\end{figure}

The dominant off-diagonal entry is 43 (Benign misclassified as DoS), corresponding to an FPR of 0.43\%---well within operational tolerances. DDoS achieves near-perfect discrimination (9964/10000 correct), consistent with its highly distinctive high-packet-rate signature. The largest confusion after Benign/DoS is between Web Attacks and Benign (18 false positives), reflecting the similarity of application-layer attack traffic to legitimate HTTP/S flows at the flow-feature level.

\section{Model Interpretability Results}

\subsection{SHAP Global Feature Importance}

Figure~\ref{fig:shap} presents the mean absolute SHAP values for the top 10 features, computed on a stratified subsample of 10{,}000 test instances using TreeExplainer.

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    xbar, width=0.92\textwidth, height=8cm,
    bar width=10pt,
    xlabel={Mean $|\phi_j|$ (SHAP value)},
    xmin=0, xmax=0.55,
    symbolic y coords={Fwd Seg Size Min,Bwd IAT Std,Fwd Header Len,Fwd Pkts/s,Pkt Length Mean,Source Port,Bwd Pkt Len Max,OutByteRate,InPktRate,Flow Duration},
    ytick=data,
    nodes near coords, nodes near coords style={font=\footnotesize},
    xmajorgrids, grid style={dotted,gray!50},
    enlarge y limits=0.06,
]
\addplot+[fill=NavyBlue!70, draw=NavyBlue] coordinates {
    (0.098,Fwd Seg Size Min)
    (0.134,Bwd IAT Std)
    (0.176,Fwd Header Len)
    (0.198,Fwd Pkts/s)
    (0.241,Pkt Length Mean)
    (0.285,Source Port)
    (0.312,Bwd Pkt Len Max)
    (0.398,OutByteRate)
    (0.441,InPktRate)
    (0.503,Flow Duration)
};
\end{axis}
\end{tikzpicture}
\caption{Mean absolute SHAP feature importance for LightGBM (top 10 features). Flow Duration, InPktRate, and OutByteRate together account for more than 40\% of the cumulative SHAP magnitude across all 74 retained features, consistent with their well-known role in volumetric and rate-based attack detection.}
\label{fig:shap}
\end{figure}

The cybersecurity interpretation of the top features:
\begin{itemize}
    \item \textbf{Flow Duration} (mean $|\phi| = 0.503$): Very short durations ($<$10~ms) correspond to port scan probes; very long durations ($>$300~s) are associated with Slowloris and SlowHTTPTest attacks.
    \item \textbf{InPktRate} (0.441): Values exceeding 5{,}000~pkts/s are strongly diagnostic of volumetric DDoS and DoS~Hulk attacks. Legitimate high-rate transfers are distinguished by their consistent byte sizes.
    \item \textbf{OutByteRate} (0.398): Asymmetrically low values relative to InPktRate signal one-directional attacks; very high values may indicate data exfiltration.
    \item \textbf{Bwd Packet Length Max} (0.312): Unusually large values may indicate exploit payloads; very small or zero values suggest SYN-flood or connection exhaustion attacks.
    \item \textbf{Source Port} (0.285): Certain attack tools use characteristic source port ranges or random ephemeral ports, providing statistical signal at the feature level.
\end{itemize}

\subsection{SHAP Dependence Analysis}

SHAP dependence plots revealed several important nonlinear relationships:
\begin{itemize}
    \item \emph{Flow Duration} exhibits a U-shaped relationship with attack probability: both very short ($<$50~ms) and very long ($>$200~s) flows carry elevated attack probability, while intermediate values typical of legitimate web sessions show low attack probability.
    \item \emph{InPktRate} increases attack probability monotonically above a threshold of approximately 3{,}000~pkts/s, consistent with volumetric attack injection rates.
    \item \emph{Interaction effects:} The combination of short Flow Duration and high InPktRate is the most strongly diagnostic signature for DoS and DDoS attacks, with interaction SHAP values significantly exceeding the sum of individual contributions.
\end{itemize}

\subsection{LIME Instance-Level Explanations}

LIME was applied to 200 randomly sampled test instances. For correctly classified DDoS instances, LIME consistently identified high InPktRate and short Flow Duration as dominant positive evidence, with Bwd Packet Length Max providing secondary corroboration.

Analysis of false-positive cases revealed a systematic pattern: legitimate high-volume file transfers exhibited InPktRate and OutByteRate values within the lower range of DoS attack distributions. This motivates the addition of application-layer protocol indicators and rolling traffic variance features in future model iterations.

Analysis of false-negative cases confirmed that Infiltration and Web attack instances with near-benign flow statistics are the primary source of misclassifications. LIME explanations show no single feature provides strong discriminative signal---the attack signature is distributed across multiple weakly informative features in a way the current feature set does not effectively aggregate.

\section{Unsupervised and Temporal Model Results}

\subsection{Isolation Forest}

The Isolation Forest model, trained in unsupervised mode on benign training flows only, achieved Recall~=~0.73 for attack detection at FPR~=~8.4\% with contamination parameter~=~0.05. While substantially lower recall than the supervised LightGBM model, the Isolation Forest successfully flagged 73\% of attack instances without requiring any labelled training data, demonstrating its value as a complementary component for zero-day threat detection.

\subsection{LSTM Temporal Model}

The LSTM model was applied to sequences of 10 consecutive flows grouped by source--destination IP pair. It achieved $F_1 = 0.94$ for combined DDoS/DoS categories and $F_1 = 0.91$ for PortScan---approximately 2 percentage points above the per-flow LightGBM model for these sequential attack types. However, for application-layer attacks (Web, Botnet, Infiltration), no improvement over per-flow classification was observed, as these attacks do not produce distinctive temporal patterns at the flow sequence level within the 10-flow window.

\section{Proposed System Architecture}

Figure~\ref{fig:arch} presents the proposed deployment architecture integrating the LightGBM classifier with enterprise SIEM platforms.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    node distance=0.55cm and 0.7cm,
    box/.style={draw=NavyBlue, rounded corners=4pt, fill=LightBlue,
                minimum width=2.5cm, minimum height=0.8cm, font=\small, align=center},
    arr/.style={-Latex, thick, NavyBlue!70},
    darr/.style={-Latex, dashed, gray!70, thick}
]
\node[box, fill=gray!15, draw=gray] (net) {Live\\Network};
\node[box, right=of net] (cap) {Packet\\Capture};
\node[box, right=of cap] (flow) {Flow\\Extraction};
\node[box, right=of flow] (prep) {Feature\\Preproc.};
\node[box, right=of prep, fill=NavyBlue!20] (cls) {LightGBM\\Classifier};

\node[box, below=1.4cm of cls, fill=AccentGreen!15, draw=AccentGreen] (expl) {SHAP +\\LIME Explainer};
\node[box, below=1.4cm of prep, fill=AccentRed!10, draw=AccentRed] (alrt) {Alert\\Generator};
\node[box, below=1.4cm of flow, fill=NavyBlue, text=white] (siem) {SIEM\\Platform};
\node[box, below=1.4cm of cap, fill=LightGray, draw=gray!70] (dash) {Analyst\\Dashboard};

\foreach \i/\j in {net/cap,cap/flow,flow/prep,prep/cls}
  \draw[arr] (\i.east) -- (\j.west);
\draw[arr] (cls.south) -- (expl.north);
\draw[arr] (cls.south) -- ++(0,-0.4) -| (alrt.north);
\draw[arr] (alrt.west) -- (siem.east);
\draw[arr] (expl.west) -- ++(-0.2,0) |- (siem.east);
\draw[arr] (siem.west) -- (dash.east);
\draw[darr] (dash.north) -- ++(0,0.5) -| node[midway,above,font=\footnotesize,color=gray] {Feedback} (prep.south);
\end{tikzpicture}
\caption{Proposed SIEM-integrated system architecture for real-time network attack detection. The forward path (top row) carries raw traffic through capture, flow extraction, preprocessing, and classification; the lower row distributes alerts and SHAP/LIME explanations to the SIEM and analyst dashboard. The dashed feedback line represents analyst-driven threshold calibration and model retraining triggering.}
\label{fig:arch}
\end{figure}

The four layers operate as follows:

\textbf{Layer 1 --- Data Ingestion.} Raw packets are captured from SPAN ports or network taps. CICFlowMeter aggregates individual packets into bidirectional flows and computes the 74 selected features per flow, producing a stream of feature vectors ready for classification.

\textbf{Layer 2 --- Classification Engine.} The trained LightGBM model processes incoming feature vectors in near-real time, producing a class probability distribution and predicted label for each flow. A configurable per-class alert threshold allows security operators to tune the precision--recall trade-off according to organisational risk tolerance.

\textbf{Layer 3 --- Interpretability Module.} For each positive detection, a SHAP explanation is computed via the pre-loaded TreeExplainer, providing a ranked list of features most responsible for the classification decision. This human-readable report accompanies each alert, enabling analysts to rapidly assess plausibility and prioritise investigation.

\textbf{Layer 4 --- SIEM Integration.} Detection events and SHAP explanations are formatted as CEF/LEEF events and transmitted to the SIEM platform via RESTful API. Within the SIEM, alerts participate in correlation rules, automated playbook triggers, threat intelligence enrichment, and incident ticket generation.

\section{Discussion of Results}

\subsection{Validation of Research Hypothesis}

The experimental results provide strong evidence supporting the central research hypothesis. LightGBM achieved $F_1 = 0.97$ and $\mathrm{AUC} = 0.99$, exceeding the predicted thresholds ($F_1 > 0.95$, $\mathrm{AUC} > 0.98$). The SHAP analysis confirmed that the model's decision process is grounded in domain-meaningful network flow attributes, substantially reducing the semantic gap between model outputs and analyst understanding identified by Sommer and Paxson~\cite{sommer2010outside}.

\subsection{Practical Significance}

The combination of high detection accuracy, fast inference, and explainable outputs makes the proposed system directly applicable to real-world network security operations. The SIEM integration architecture provides a concrete blueprint for operationalisation, and the interpretability outputs---SHAP summaries, LIME explanations, feature importance rankings---are directly usable as analyst decision-support tools during alert triage and incident investigation. A rough cost-effectiveness calculation, based on the typical analyst time savings reported in industry surveys (15--25\% of triage time when SHAP-style explanations accompany alerts), suggests that the interpretability component alone may justify the deployment of the system in any organisation with a security operations centre handling more than a few thousand alerts per day.

\subsection{Limitations}

The primary limitation is the use of a single benchmark dataset generated in 2017 under controlled laboratory conditions. Real-world network traffic exhibits substantially greater diversity and temporal non-stationarity; flow features drift over months and years as application protocols evolve, encryption usage spreads, and user behaviour shifts. The model's performance on post-2017 attack techniques---AI-generated traffic, supply-chain compromise, zero-trust evasion, encrypted command-and-control over modern transports such as DNS-over-HTTPS or QUIC---remains to be evaluated. Furthermore, Infiltration detection ($F_1 = 0.885$) highlights the inherent difficulty of identifying attacks that are indistinguishable from benign traffic at the flow-feature level without additional contextual information such as endpoint telemetry, user-behaviour baselines, or threat-intelligence enrichment. Finally, all results reported here are obtained on a single deterministic train/validation/test split with fixed random seed; while this is standard practice for reproducibility, it does not capture run-to-run variance, which a future repeated-experiment campaign would quantify.
